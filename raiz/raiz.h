

/* determinação de raiz pelo método da secante */
int secante (double x0, double x1, int p, double (*f) (double x), double* r);

int IQI (double x0, double x1, double x2, int p, double (*f) (double x), double* r);
